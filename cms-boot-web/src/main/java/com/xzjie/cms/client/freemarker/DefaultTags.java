package com.xzjie.cms.client.freemarker;

import freemarker.template.ObjectWrapper;
import freemarker.template.SimpleHash;

public class DefaultTags extends SimpleHash {

    public DefaultTags(ObjectWrapper wrapper) {
        super(wrapper);
    }
}
